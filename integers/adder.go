package integers

// Adds two integers and returns the result
// Note: when you have more than one argument of the same type you can shorting from
// (a int, b int) to (a, b int)
func Add(a, b int) int {
	return a + b
}
